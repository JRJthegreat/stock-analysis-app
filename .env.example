# Public-safe Supabase values — inlined into the client by Next (NEXT_PUBLIC_*), NOT secrets.
# Copy to .env and fill in after the Supabase edge functions are deployed.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
