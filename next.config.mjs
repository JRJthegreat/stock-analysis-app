/** @type {import('next').NextConfig} */
const nextConfig = {
  // The pure analysis engine in src/engine/ is plain TypeScript and ports here
  // unchanged. All network/I-O stays in src/data/. Nothing else special needed.
};

export default nextConfig;
